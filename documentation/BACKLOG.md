# Backlog

> **What this doc is for:** Tracks deferred follow-up work — findings, gaps, or manual actions that were identified but not fully resolved. Add an entry whenever work is knowingly deferred instead of finished; remove/move to "Done" once actually resolved (link the commit).
>
> Referenced from `.claude/CLAUDE.md` — check this file at session start alongside the doc index.

---

## Open

### BL-18 — Gameplay Jev extractor sends `noul` criteria as a list; live API rejects it with HTTP 422 (source: Jev game arena build, 2026-09-23)
**What:** `engine/extractors/decision_registry.py` builds every `noul` Decision (`knowledge_decision`, speaker nouls, `relationship_history_decision`, ...) with `criteria=[...]` (a list). `JevClient._build_criteria` passes lists through unchanged. Verified live on 2026-09-23: `knowledge_decision("c1", ...)` -> `HTTP 422 ... questions.knows_c1.noul.criteria: Input should be a valid dictionary`. The documented shape (`JEV_EXTRACTOR_REDESIGN_2026_09_22.md` §8) is a `{"true": ..., "false": ...}` map. Consequence: any Jev batch containing one of these questions fails as a whole and the resolver falls back to legacy, so those abilities are never actually Jev-answered on beta (and the failures feed the circuit breaker).
**Why deferred:** Found while building the evaluation arena; the fix changes live extractor behavior in another agent's in-flight Jev rollout (BL-17), so it needs that owner's decision and its own ship-and-verify pass rather than riding along with the arena commit.
**What's needed:** Map list criteria for `noul` to `{"true": criteria[0], "false": "not: " + criteria[0]}` in ONE place (`JevClient._build_criteria`), add a regression test that asserts the request shape, and re-verify live that the knowledge/speaker/relationship-history batches return answers. The arena's own probes already use the map shape (`evaluation/rubric.py` `CriticalProbe.criteria`).
**Touches:** `backend/app/llm/providers/jev.py`, `tests/backend/app/llm/providers/`.

### BL-19 — Arena Phase 2: server receipts, controlled init, snapshots, response forks (source: Jev game arena, 2026-09-23)
**What:** The arena runs in observational mode: public `/api/chat` + the player-facing `[D]` debug box. Not yet built: server-side per-turn receipts (applied events, pre/post state hashes, component versions), seeded/controlled initialization (roster, RNG streams), snapshot export/restore, and the identical-state response-fork track (design §5B). Checks needing them (`cast_capacity`, `rng_stream_parity`, `state_transition_legality`, `snapshot_round_trip`) are reported as not measured.
**Why deferred:** These belong with the Phase 2 `SessionFactory`/`SnapshotCodec`/`TurnService` restructure (`PHASE_2_BACKEND_RESTRUCTURE_DESIGN_2026_09_22.md`) rather than being bolted onto the HTTP handler; prod also needs them deployed before controlled hosted parity is possible (explicit release decision).
**What's needed:** Implement receipts + controlled init + snapshot codec behind operator auth, flip the flags in `api/eval_capabilities.py`, then add a `ResponseForkRunner` and the unsupported checks.
**Touches:** `backend/app/api/prompt_engine.py` (via Phase 2 services), `backend/app/api/eval_capabilities.py`, `backend/app/evaluation/`.

### BL-20 — Arena judge needs human calibration before any verdict is more than advisory (source: Jev game arena, 2026-09-23; owner action)
**What:** Rubric weights, the episode margin (0.55) and the critical-probe threshold (0.5) are the design's proposals, not calibrated values. Mutation sensitivity + A/A are implemented, but agreement with human judgment is unmeasured, so every report is labeled "uncalibrated: advisory only".
**Why deferred:** Requires ~200 human-labeled pairs (two labelers on ambiguous cases, adjudicated) — an owner action, not code.
**What's needed:** Label pairs from real arena artifacts using `evaluation/calibration.py` `HUMAN_LABEL_FIELDS` (JSONL), add an agreement report (per-dimension agreement, severe-error recall/precision, order sensitivity), then freeze weights/thresholds and bump `calibration_version`.
**Touches:** `backend/app/evaluation/calibration.py`, `rubric.py`.

### BL-21 — Arena post-deploy trigger and hosted results view (source: Jev game arena, 2026-09-23)
**What:** Arena runs are launched manually from the CLI and produce `report.json`/`report.html` locally. Design §11 step 5 wants an opt-in run after each beta deploy and an operator results page.
**Why deferred:** Needs a durable worker/queue on Railway and an operator-only page; the CLI + report cover the first useful milestone.
**What's needed:** Operator-only endpoint/page serving stored reports; opt-in post-deploy job with the manifest budget ceilings.
**Touches:** `backend/app/evaluation/`, new operator route, `frontend/`.

### BL-15 — Workload harness needs the full matrix run (source: Phase 0B, 2026-09-22)
**What:** `scripts/bench/turn_workload_harness.py` (Phase 0B) is proven working against live beta but was only run at a small scale: one story, concurrency 1/3, 2 sessions per level. The plan specifies both stories, cold/warm start, short/long conversations, movement, time skip, queue exhaustion, provider errors, and concurrency 1/10/50.
**Why deferred:** Each additional run spends real paid LLM tokens against the live provider; running the full matrix casually was out of scope for proving the harness itself works. Concurrency 10/50 in particular are real paid-call bursts that deserve a deliberate window, not an unattended background run.
**What's needed:** Run `--concurrency 10 50`, add the second story, and add the scenario types listed above once there is a deliberate budget/window for it. See `documentation/PHASE_0B_BASELINE_NOTES_2026_09_22.md` for the current partial baseline and exact gaps.
**Touches:** `scripts/bench/turn_workload_harness.py` (also needs `cached_tokens` added to its summary — currently only captured manually, not in the harness's own report).


### BL-14 — GitHub webhook secret is recoverable from git history; needs rotation or webhook deletion (source: Engineering plan Phase 0A, 2026-09-21)
**What:** `deployment/gitwebhook.php` contained a literal HMAC signing secret and was a **tracked** file, so the value is present in git history across at least `2caee48`, `57c1a9a`, `612d104`, `0fa402e`, `0578a87`. Phase 0A quarantined the file to `deployment/legacy_cpanel_unused/gitwebhook.php.disabled`, removed the literal (now `getenv("GITWEBHOOK_SECRET")`), and made it fail closed with 503 when unset. **Removing it from the working tree does NOT remove it from history** — anyone with repo read access can still recover the original value.
**Why deferred:** Rotation requires GitHub repository-settings access (Settings → Webhooks), which the agent does not hold. Code-side remediation is complete; this is the owner action that remains.
**What's needed:** Verify empirically-confirmed-dead status holds (Phase 0A established that Railway serves both `storieschat.ai` and `beta-api.storieschat.ai` — `x-railway-edge` headers, FastAPI serves the frontend directly — so the cPanel/PHP path has no live listener). Then **delete the webhook** in GitHub settings (preferred, since it is now redundant), or rotate its secret and set `GITWEBHOOK_SECRET` on the host. History rewrite was considered and rejected as default: it would break the parallel-agent workflow in CLAUDE.md §7. Rotation makes the leaked value inert, which is sufficient.
**Touches:** GitHub repo settings only (no code change outstanding). See `deployment/legacy_cpanel_unused/README_DISABLED.md`.


### BL-13 — Full iOS/mobile UI overhaul plan not actioned (source: separate prior-session audit, `documentation/IOS_UI_SMOOTHNESS_PLAN_2026_09_19.md`, deferred 2026-09-20)
**What:** An untracked prior-session document (`documentation/IOS_UI_SMOOTHNESS_PLAN_2026_09_19.md`, plus screenshots in `documentation/ui_audit_2026_09_19/`) proposes a large, multi-delivery mobile/iOS UI overhaul: keyboard-safe chat shell, typing-animation rework (remove mandatory character-by-character reveal), map/locations sheet redesign, touch-target sizing, composer text-size, and more. Re-verified live on beta at 390px (guest session, six_strangers) during Phase 4 of the Six Strangers audit: the doc's P1 "broken portrait images (404)" finding did **not** reproduce — all 6 cast roster portraits and the onboarding character-picker portrait loaded correctly (non-zero `naturalWidth`) from `beta-api.storieschat.ai`; roster text/layout at 390px also looked reasonably readable, not the "tiny, faint, squeezed" state described. These specific items appear to have already been fixed since that audit was written. The remaining items (keyboard-safe shell, typing-animation removal, map/sheet rework, placeholder-control cleanup) were not verified and not actioned.
**Why deferred:** Out of scope for Phase 4's "mobile/portrait interaction polish" line item, which was meant as a light inspection/polish pass, not a full UI-overhaul delivery. User explicitly chose the "small Phase 4 polish only" option when asked, with this doc's larger plan to remain a separate tracked item rather than be silently absorbed into this session's Phase 4 commit.
**What's needed:** Before starting this doc's plan, re-run its own audit steps fresh (many findings may be stale, as the portrait issue was) to confirm which items are still live problems. Then treat it as its own multi-commit initiative with the project's normal ship-and-verify cadence, not a single pass.
**Touches:** `frontend/index.html` (chat shell, composer, map modal, typing animation) — scope TBD pending re-audit.

### BL-12 — Short-message pacing instruction improves but doesn't fully hit its own hard cap (source: Six Strangers audit Phase 4, 2026-09-20)
**What:** The "PACING AND INITIATIVE" section (`backend/app/engine/prompt_builder.py`, `system_prompt()`) states a hard 3-4 sentence cap for short/low-stakes player messages. Live-verified on beta (six_strangers, real guest sessions): replies to "hi"/"ok" dropped from ~142-178 completion tokens (pre-change baseline observed earlier in this same session) to ~120-126 tokens after the strengthened instruction — real, measurable improvement — but the model still tends to produce 2 paragraphs rather than the stated 1-paragraph/3-4-sentence target.
**Why deferred:** Pure prompt-wording iteration has diminishing returns here — the pacing instruction is competing against other earlier, structurally strong instructions (STORYTELLER CONTRACT's "compact cinematic paragraphs," persona/cast-pressure framing encouraging a populated scene) for the model's attention. Further tuning likely needs a different lever than more emphatic wording — e.g. a dynamically lowered `max_tokens` for detected-short messages, or moving the pacing section even later/more prominently in the prompt, or accepting the current improvement as sufficient. User explicitly accepted the current state as real progress rather than pursuing further prompt-only iteration in this pass.
**What's needed:** If tighter compliance is wanted later, try a non-prompt-only lever (e.g. `MAX_TOKENS` reduced conditionally in `prompt_engine.py`'s API call when the player's message is short) rather than further prompt-wording changes.
**Touches:** `backend/app/engine/prompt_builder.py`, `backend/app/api/prompt_engine.py`.

### BL-11 — NPC-to-NPC relationship edges are not authored, so disposition shifts only affect player-facing pairs today (source: Six Strangers audit Phase 3, 2026-09-20)
**What:** `SocialShiftSignal`'s apply logic (`backend/app/api/prompt_engine.py`) correctly requires an ALREADY-ESTABLISHED `RelationshipEdge` before it will attach a `disposition` to a pair — it never fabricates a new edge. Six Strangers' story JSON only authors `player->X` edges (confirmed live: `character_graph.get_edge("makoto", "mizuki")` is `None`), so an NPC-to-NPC disposition shift (e.g. Makoto's feelings toward Mizuki evolving, independent of the player) is mechanically supported by the engine but has nothing to attach to in the current story content.
**Why deferred:** This is a content/authoring gap, not an engine gap — fixing it means adding NPC-NPC edges to `six_strangers_story.json`'s `relationships.edges` block (same shape as the existing player-facing ones), which is story-writing work, not code. The apply pipeline's safe-no-op behavior when no edge exists was deliberately verified and locked in by a dedicated test (`test_social_shift_disposition_no_edge_is_safe_noop`) rather than treated as a bug.
**What's needed:** Author NPC-NPC `RelationshipEdge` entries in `six_strangers_story.json` (and any future story wanting this) for the specific pairs whose evolving relationships matter to the narrative — at that point `SocialShiftSignal` will work for those pairs with zero engine changes.
**Touches:** `backend/app/stories/7_six_strangers/six_strangers_story.json` (content only).

### BL-04 — `frontend/debug.html` doesn't send the new operator token (source: audit A03 follow-up)
**What:** Debug/playback/authoring routes now require `X-Operator-Token` (commit `f443238`), but the hosted debug UI (`frontend/debug.html`) was never updated to send it, so it will 401 against a real deployment with `DEBUG_TOOLS_ENABLED=1`.
**Why deferred:** Out of scope for the security-fix pass (frontend UI wiring, not a vulnerability).
**What's needed:** Add an operator-token input/storage to `debug.html` and send it as `X-Operator-Token` (REST) / `operator_token` query param (WebSocket) on every debug/playback request.
**Touches:** `frontend/debug.html`.

### BL-05 — Rotate any API key that went through the old Docker build-arg path (source: audit A11)
**What:** Before commit `1cf4c9d`, the Dockerfile persisted an `OPENAI_API_KEY` build ARG into image `ENV`, meaning any image actually built that way has the key baked into its config/layers.
**Why deferred:** Not something an agent should do — key rotation is an owner action.
**What's needed:** If that build path was ever run with a real key, rotate the key in the provider dashboard and update the deployment secret.
**Owner action required:** yes.

### BL-06 — NPC→player and NPC↔NPC relationship drift is not mechanically driven (source: social_sim mode + Common Room work, 2026-09-17)
**What:** The numeric relationship extractor (see `backend/app/engine/prompt_builder.py` relationship layer and `backend/app/api/prompt_engine.py` turn extraction) only accepts player→NPC updates today; only the main NPC has a narrative-tag-driven affection path. `relationships.edges` in story JSON seed the *initial* state for all directed pairs (including NPC-NPC edges, as used in `backend/app/stories/6_common_room/`), and player-driven changes to player-facing edges are supported, but nothing updates NPC-initiated or NPC-NPC edges during play.
**Why deferred:** A genuinely dynamic ensemble house sim (e.g. Mina and Dae-ho's friendship visibly drifting from something the player does) needs this, but it's a meaningful extension of the turn-extraction schema, not a small patch, and was out of scope for adding the `mode` layer itself.
**What's needed:** Extend the single-call turn extractor's schema/prompt to propose NPC↔NPC and NPC→player relationship deltas (not just player→NPC), and apply them the same way player-facing deltas are applied today.
**Touches:** `backend/app/engine/extractors/turn_extractor.py`, `backend/app/api/prompt_engine.py`, `backend/app/engine/character_graph.py`.

### BL-08 — No quest/schedule/resource engine (source: social_sim mode + Common Room work, 2026-09-17; pre-existing gap)
**What:** `backend/app/engine/gameplay.py` has no scripted event loop, quest triggers, schedule/job simulation, or resource tracking. A Terrace-House-style social sim game is built entirely from free-form roleplay + `character_self_knowledge` + `epistemic_seed` + the relationship graph + the location/travel graph — see `documentation/model_output_docs/SOCIAL_MODE_DESIGN.md` and `documentation/model_output_docs/GAME_DESIGN_SYSTEMS.md`. Chores/jobs/daily rhythms in story content (e.g. Common Room's chore wheel) are narrative/roleplay content the NPCs can talk about, not mechanically enforced systems.
**Why deferred:** A real scheduling/quest/resource engine is a substantial, genre-spanning feature, not something to bolt on while adding one story mode layer.
**What's needed:** Design a lightweight, story-JSON-declarable schedule/trigger primitive (e.g. NPC location-by-time-of-day, simple flag-gated triggers) that both mystery and social_sim stories could opt into.
**Touches:** `backend/app/engine/gameplay.py`, story JSON schema, `documentation/model_output_docs/GAME_DESIGN_SYSTEMS.md`.

### BL-10 — Hosted beta still serving stale story catalog despite repo being correct (source: live verification, 2026-09-19)
**Latest verification (2026-09-19):** No longer reproduces: Railway deployment `23b0daa3-f2b6-4302-9ec1-e822f2f122da` succeeded for `c3e2a19`; live beta returns only `iu_murder_mystery` and `six_strangers`, and `[CAST]` returns the structured zero-token roster. Historical cause was not established. Keep deployment build identity and acceptance checks as follow-up work. See [audit and proposal](SIX_STRANGERS_AUDIT_PROPOSAL_2026_09_19.md) for current evidence and gameplay findings; the older observations below are historical.
**What:** The repo code and local validation are correct: `build_story_registry()` resolves only `iu_murder_mystery` and `six_strangers`, and the Python test suite passes. The live beta API at `https://beta-api.storieschat.ai/api/stories`, however, still returns the old catalog with inactive story IDs (`jennie_murder_mini`, `blackout_manor`, etc.), which means the deployment environment is not reflecting the repo state.
**Why deferred:** This is not a repo-side logic bug in the current branch; it appears to be an infra/deploy freshness issue in the hosted beta environment. The real fix is external to the codebase: a fresh Railway build/redeploy or a clean environment rebuild.
**What's needed:** Re-trigger the beta deployment from the actual hosting environment, confirm the rebuilt container is serving the repo state, and re-run the live curl/browser smoke check against the real hosted endpoints before claiming the push is accepted.
**Touches:** deployment environment / Railway, hosted beta API, `frontend/index.html` smoke verification.

---

## Done

_(move resolved items here with the commit SHA that closed them)_

### BL-17 — Jev integration steps 6-9 — CLOSED (2026-09-23)
**What was done:** All 9 steps of `documentation/JEV_PROVIDER_ARCHITECTURE_2026_09_22.md` §12's implementation order are shipped: all 12 `TurnExtraction` abilities are now registered as `Decision` objects (movement_intent, movement_destination, prev_scene_location, knowledge fan-out, departure fan-out, rel_state fan-out, rel_history fan-out, behavior_tag fan-out, social_shift certainty), each routed through Jev with the legacy generative call as fallback. Steps 8-9 (behavior_tags, social_shift) were unblocked by fixing BL-16 first (below) — behavior_tag_decision and social_shift_certainty_decision both depend on the closed vocabulary BL-16 introduces. `TurnExtractor._build_batches`/`_apply_decision_overrides` gained one batch + one override method per ability; `Decision.legacy_resolver` (added in steps 6-7) covers every fan-out shape needed. Ability 12's `new_value` free text still requires the legacy generative call — `_apply_social_shift_override` only applies a Jev SHIFT when legacy's own answer corroborates it with a non-empty `new_value`, otherwise degrades to whatever legacy alone produced (never fabricates a SHIFT with no description). The "hybrid trap" (legacy call still runs unconditionally once `outcome.legacy_raw is None`, for ability 12's new_value) remains open by design — a future step to shrink the legacy prompt once safe is still out of scope.
**Tests:** 46 tests in `tests/backend/app/engine/extractors/test_turn_extractor_jev_seam.py` (up from 0 before steps 6-9), 4 new tests in `tests/backend/app/llm/decisions/test_resolver.py` for the `legacy_resolver` mechanism. Full suite: 972 passed, 1 xfailed (was 944 before steps 6-9 began).
**Touches:** `backend/app/llm/decisions/types.py`, `backend/app/llm/decisions/resolver.py`, `backend/app/engine/extractors/decision_registry.py`, `backend/app/engine/extractors/turn_extractor.py`.

### BL-16 — `_ripe_behavior_pairs` false positives on free-text tag synonym drift — CLOSED (2026-09-23)
**What was done:** Added a story-authored `behavior_tag_vocabulary: list[str]` field, threaded through `_canonicalize_story_cfg` (`backend/app/api/prompt_engine.py`) alongside the existing `canonical_truth` list-field pattern. `TurnExtractor._parse_dict` now takes `allowed_behavior_tags`; when non-empty, an emitted tag not in the vocabulary is dropped (case-insensitive match, canonicalized to the vocabulary's own casing) rather than passed through — matching every other "unknown -> drop" rule already in that parser. The legacy prompt's rule 9 is rewritten dynamically to list the allowed tags and instruct the LLM to pick from them when a vocabulary exists, falling back to the original free-text rule byte-for-byte when it doesn't (every pre-existing story, until one is authored). Session restore gained `_migrate_behavior_log_to_vocabulary`: drops (not fuzzy-maps) any previously-saved free-text tags that don't match a newly-authored vocabulary — the simplest correct choice given the log's small (<=8 entries/pair) rolling window. `_ripe_behavior_pairs` itself is unchanged, as originally documented: the fix is upstream at tag production, and a closed vocabulary is what makes `Counter.most_common(1)`'s exact-string majority vote meaningful instead of near-certain-arbitrary.
**Tests:** 6 new tests in `tests/backend/app/engine/extractors/test_turn_extractor.py` (vocabulary keep/drop/case-insensitive/mixed-batch/empty-accepts-anything), 6 new tests in `tests/backend/app/api/test_prompt_engine.py` (`_canonicalize_story_cfg` passthrough, `_migrate_behavior_log_to_vocabulary`, and a dedicated end-to-end regression proving 6 casing variants of the same tag canonicalize identically and no longer read as a false-positive majority swing).
**Touches:** `backend/app/api/prompt_engine.py`, `backend/app/engine/extractors/turn_extractor.py`, `backend/app/llm/protocols.py` (`LegacyExtractionRequest.allowed_behavior_tags`).

### BL-01c — Fact-extraction outbox mid-uptime recovery for hung (not crashed) tasks — CLOSED (2026-09-22, Phase 1.2 of the reuse/performance engineering plan)
**What was done:** `FactExtractionOutboxRepo.claim_pending()` (`backend/app/db/repos.py`) atomically leases pending rows (`lease_until` column, added via `_ensure_column` migration so existing deployed DBs pick it up without a destructive `ALTER`). A new `_fact_extraction_periodic_sweep_loop()` in `backend/app/main.py` runs every 10 minutes (`FACT_EXTRACTION_SWEEP_INTERVAL_SECONDS`), claiming rows whose lease has expired (5-minute lease, `FACT_EXTRACTION_LEASE_SECONDS`) and reprocessing them through the same durable path the startup sweep uses (`_reprocess_outbox_rows`, shared between both sweeps). Wired into `lifespan` alongside the existing guest-cleanup task.
**Tests:** `test_claim_pending_sets_lease_and_hides_row_from_immediate_reclaim`, `test_claim_pending_reclaims_after_lease_expires`, `test_claim_pending_does_not_claim_done_or_failed_rows` (`tests/backend/app/db/test_repos.py`); `test_fact_extraction_periodic_sweep_reclaims_hung_task` (`tests/backend/app/test_main.py`) — simulates a row left pending by a hung (not crashed) task via an already-expired lease, proves the periodic sweep's claim step reclaims it and resolves it.
**Touches:** `backend/app/db/database.py`, `backend/app/db/repos.py`, `backend/app/main.py`.

### Six Strangers audit Phase 4 — engagement and polish — FULLY COMPLETE (2026-09-20, source: [audit and proposal](SIX_STRANGERS_AUDIT_PROPOSAL_2026_09_19.md))
**Status:** all four items in the user's confirmed sequencing (response/initiative tuning → journal/recap UI → time-skip UI → mobile/portrait polish) are done and live-verified.
1. **`b7e3119`/`4b56472`/`a2134a6`** — response-length/NPC-initiative pacing tuning. See BL-12 (open) for the honest partial-compliance finding; user explicitly accepted current state and asked to move on.
2. **`93a1892`** — journal/recap UI, auto-generated entirely from existing Phase 3 engine state per the user's explicit confirmed design choice (no new extractor signal). New `GET /api/user/sessions/{session_id}/journal` endpoint (`backend/app/api/user_sessions.py`) walks `state.characters[*].goal.history` and `state.character_graph.edges[*].disposition.history`/`.narrative_log`, resolves character keys to display names, sorts by `timestamp_minute`. Reuses the exact auth/ownership pattern of the pre-existing `/history` endpoint. Frontend: new "Journal" entry in the chat 3-dot dropdown opens a full-screen `#journal-modal` (styled after the existing `#map-modal` fullscreen-overlay pattern), fetched via the existing `apiHeaders()`/`USER_SESSIONS_URL` helpers. Entry text is rendered via `textContent`, not `innerHTML` string-building, since it's LLM/server-sourced content. **Live result:** verified end-to-end on beta with a real guest session on `six_strangers` — curled the new endpoint directly (16 authored goal entries returned, correctly minute-sorted and name-resolved) and separately drove it through the actual browser UI (Playwright against `https://storieschat.ai/beta/`): opened the 3-dot menu, clicked Journal, confirmed all 16 cast goal entries rendered in the modal, confirmed the close button returns cleanly to the chat screen with zero console errors.
   - Tests added: `tests/backend/app/api/test_user_sessions.py` — 5 new tests (auth required, 404 on missing/foreign session, goal+disposition+narrative_log entries returned with resolved names and minute-sorted order, empty-list graceful degradation for a goal-less character). Uses real `Character`/`EvolvingTrait`/`RelationshipEdge`/`CharacterGraph` objects, only the DB-ownership boundary mocked.
3. **`5024345`** (time-skip UI) + **`fa7e77b`** (narration-cue fix) + **`fde847a`** (build-contamination revert, see incident note below) — lets the player jump the world clock forward ("a few hours"/"overnight"/"a full day") from a new "Skip Ahead" action sheet in the chat 3-dot menu, consistent with the North Star's "events continue off-screen" principle: a skip produces a real narrated turn, not a silent state mutation. New `advance_time_by(state, minutes)` in `gameplay.py` (a pure arbitrary-delta clock jump, distinct from the per-turn `advance_time()`). New `__cmd_skip__:<PRESET>` command recognized early in `_chat_handler_impl`, falls through into the normal turn pipeline with a narration cue rather than returning a static toggle box. Reuses the Phase 2 `process_pending_events()` scheduler as-is (confirmed its day-boundary check is a `<=` scan, safe for a large jump in one call — proven live: a skip crossing a scheduled cast-replacement's day boundary correctly fired the replacement). **Two real bugs found and fixed before/immediately after shipping:** (a) the narration cue text has the same shape as `extract_user_name_from_text()`'s bare-word "solo name" fallback regex and would silently corrupt `state.user.formal_name` — caught by a dedicated test before shipping, verified the test actually fails without the guard by temporarily reverting it; (b) **live-verified after the first deploy** that a generic cue ("The night passes") was silently ignored by the story master — it kept narrating the OLD time of day even though `state.minute` had genuinely advanced (confirmed via the debug box), because `WorldTimeFormatter`'s timestamp is only ever surfaced in the debug box, never in the actual LLM prompt. Fixed by embedding the resolved post-skip timestamp directly in the narration cue (`"[Time skip] The night passes. It is now 2025-01-02 04:06 AM."`) — re-verified live: an OVERNIGHT skip now correctly produces morning-appropriate narration ("The glow of early morning... I didn't expect to be up this early").
   - Tests added: `tests/backend/app/engine/test_gameplay.py` (3 new: `advance_time_by` minute-jump/no-op/world-clock-sync) + `tests/backend/app/api/test_prompt_engine.py` (7 new: preset token parsing, end-to-end HOURS/OVERNIGHT/DAY skips via real `/api/chat` calls, unknown-preset safe fallthrough, the name-corruption regression, the explicit-new-time-in-cue regression, and the scheduler-composition test).
   - **Incident during this commit:** while committing the narration-cue fix, `git add backend/app/api/prompt_engine.py` staged the entire current working-tree content of that shared file — which at that moment also held another concurrent session's unstaged, unrelated, in-progress "speaker-aware dialogue presentation" feature (a new `backend/app/engine/dialogue.py` module, not yet committed). The push (`fa7e77b`) broke the Railway build (`ModuleNotFoundError: No module named 'backend.app.engine.dialogue'`, deployment `81351e65`, FAILED). Diagnosed via `railway logs -b`, fixed by restoring `prompt_engine.py` to the last commit confirmed clean of that import and manually re-applying only the intended narration-cue diff on top (verified via `git diff <bad-commit> -- prompt_engine.py` that the revert touched nothing else), committed as `fde847a`, redeployed successfully (`462d637b`, SUCCESS). The other session's actual work was never lost — it remained correctly unstaged in the working tree throughout and was not touched by the fix. **Lesson:** when a file is known to be under concurrent edit by another session, `git add <path>` is unsafe — it stages that file's entire current content, not a diff of only your own change. Review with `git diff <path>` first, or use `git add -p` to select only your own hunks.
4. **Mobile/portrait polish** (scoped per explicit user choice: a small polish pass, not the full separate iOS overhaul plan — see BL-13) — live re-verified at 390px width on beta that the pre-existing `documentation/IOS_UI_SMOOTHNESS_PLAN_2026_09_19.md` audit's P1 "broken portrait 404" finding no longer reproduces (all 6 cast roster portraits + the onboarding character-picker portrait load with valid `naturalWidth` from `beta-api.storieschat.ai`) — already fixed by an earlier, unrelated commit (`beaa8c4`) before this session started. One concrete, low-risk fix applied: composer textarea `font-size` 15px → 16px (`frontend/index.html`), preventing iOS Safari's well-documented auto-zoom-on-input-focus behavior. The full iOS overhaul plan (keyboard-safe shell, typing-animation rework, map/sheet redesign) stays deferred as BL-13 rather than silently expanding this session's Phase 4 scope.
- Full suite at Phase 4's final commit (`fde847a`): 664 passed, 1 xfailed, 0 failed (includes tests from the other session's concurrent, still-uncommitted work present in the local working tree at that time; unaffected by and independent of this phase's own changes).

### Six Strangers audit Phase 3 — social life (evolving NPC goals & dispositions) — FULLY COMPLETE (2026-09-20, source: [audit and proposal](SIX_STRANGERS_AUDIT_PROPOSAL_2026_09_19.md), user request for reusable evolving-goal design)
**Status:** every NPC now always "knows" a persistent goal seeded from story authoring, can form an evolving disposition toward a specific other character, and both can genuinely shift based on accumulated in-story behavior over several turns — not a single line of dialogue or player assertion. Designed as a single genre-agnostic engine primitive (`EvolvingTrait`) explicitly built to serve both an ensemble drama's romance/motive arcs and a murder mystery's `motive`+`tells`-driven suspicion arcs identically, with zero per-genre branching anywhere in the engine. Ships as three independently live-verified commits, same cadence as Phases 1-2.
**Resolved by:** `5956fca` (foundation + rendering), `32f0338` (cheap behavior tagging + ripe-window heuristic), `92b0090` (SocialShiftSignal judgment + apply pipeline).
1. **`5956fca`** — new `backend/app/engine/social_traits.py`: `EvolvingTrait`, a labeled current value plus an append-only, provenanced history (reusing the existing generic `KnowledgeChunk` record type rather than inventing a second one). Fixed a REAL, PRE-EXISTING DROP-BUG found while researching this phase: both real stories (`six_strangers`, `iu_murder_mystery`) already author a per-character `"motive"` string (murder mystery adds `"tells"`), but `Character.from_dict`'s `known_keys` whitelist silently dropped both into an inert `meta` bucket that zero code anywhere read. Wired `Character.goal`/`Character.tells` and `RelationshipEdge.disposition` (same primitive, edge-scoped) end to end: seeding, prompt rendering (`_character_identity_section`, `CharacterGraph.format_for_prompt`), and serialize/restore. **A second real bug was found and fixed in the same commit:** the newgame AND session-restore character-construction loops each built a fresh `Character(...)` from the loaded story character but never copied `goal`/`tells` across, so a freshly-seeded goal would silently vanish the moment a session restored — caught by a dedicated round-trip test before shipping, not discovered live. **Live result:** Makoto's authored motive ("make real friends... without losing sight of baseball") visibly shaped his live in-character dialogue on beta, confirmed via a direct probe — proof the full pipeline (story JSON → seeding → serialization → prompt rendering) works end to end, not just in unit tests.
2. **`32f0338`** — `TurnExtractor` gained cheap, always-on per-turn `behavior_tags` (free-text, no enum — genre-agnostic) accumulated into `GameState.recent_behavior_log`, plus a pure, no-LLM `_ripe_behavior_pairs()` heuristic deciding which pairs have accumulated a genuine majority swing (not a single outlier tag) worth an expensive shift judgment later. Nothing acts on anything yet in this commit — deliberately kept inert and independently verifiable. **Live result:** zero errors, zero behavior change (as intended), Phase 1/2 regressions confirmed still holding on the merged deploy.
3. **`92b0090`** — `SocialShiftSignal` (categorical `NONE|WISH|SHIFT`, mirroring Phase 2's `DepartureSignal` precedent exactly — "is this a settled change or a momentary flicker" is a discrete call, not a numeric threshold) is only ever requested from the extractor when a ripe pair exists (most turns: zero added prompt tokens). A confirmed `SHIFT` is re-validated against live state (a goal shift needs a real `Character`; a disposition shift needs an ALREADY-ESTABLISHED relationship edge — never fabricates a new one) and applied via `propose_change()` with a turn-keyed idempotent entry_id, appending a new history entry without ever overwriting the old value. **Live result:** deploy healthy, both Phase 1/2 live regressions (future-resident unfamiliarity, solitude) confirmed still holding on the fully-merged tree, zero errors across the verification session. The actual multi-turn shift-judgment mechanism itself is verified by mocked end-to-end tests locally (matching Phase 2's own established precedent for `departure_signal` — reliably triggering a genuine multi-turn LLM judgment deterministically live/in CI is impractical), not forced live, since doing so would need many real consistent-tone turns; this is the same tier of verification the project already accepted for the structurally identical `departure_signal` mechanism in Phase 2.
- **Build-race discovered mid-phase (not a code bug):** the second commit's first deploy attempt failed the Railway build's full-suite gate on an unrelated test (`test_japanese_language_theme_survives_canonicalization_and_styles_pauls_name`) that passed reliably twice locally. Root cause: another session pushed `e0f64a1` (a Japanese-language-theme feature touching that exact code path) to `beta` concurrently — the failed build had raced against a partially-landed push. Confirmed by pulling `e0f64a1`, rerunning the full suite locally (629 passed clean), and rebuilding — succeeded immediately. **Lesson:** a build failure on a file/test you didn't touch, immediately after a push, is worth checking for a concurrent push from another session before assuming your own change broke something — `git log HEAD..origin/beta` is the first thing to check.
- Tests added: `tests/backend/app/engine/test_social_traits.py` (new file); extended `tests/backend/app/engine/test_state.py`, `tests/backend/app/engine/test_character_graph.py`, `tests/backend/app/engine/test_prompt_builder.py`, `tests/backend/app/engine/extractors/test_turn_extractor.py`, `tests/backend/app/api/test_prompt_engine.py` — covering initial seeding from both real stories, graceful degradation for goal-less/disposition-less content, full history preservation (never overwrite) through both live mutation and save/restore round-trips, the WISH-vs-SHIFT false-positive guard, retry idempotency, the ripe-window heuristic in isolation, and the safe-no-op behavior when a disposition shift names a pair with no existing relationship edge (see BL-11).
- Full suite at final commit: 637 passed, 1 xfailed, 0 failed.
**Deliberately not done (see BL-11):** NPC-to-NPC relationship edges are not yet authored in `six_strangers_story.json` (only `player->X` edges exist), so disposition shifts for non-player pairs are mechanically supported but currently inert content-side — a story-authoring gap, not an engine gap.

### Six Strangers audit Phase 2 — complete lifecycle — FULLY COMPLETE (2026-09-19, source: [audit and proposal](SIX_STRANGERS_AUDIT_PROPOSAL_2026_09_19.md))
**Status:** cast cycling is now a real, playable feature, not just a tested-but-unreachable foundation. A resident character's own stated, serious decision to leave schedules a deferred replacement at the authored availability window (`replacement_timing: "next_day"`, previously parsed but inert); the replacement executes automatically once that day boundary passes; the arriving successor gets a seeded relationship edge to the player and a fresh-introduction framing in the next narration; the whole chain survives a save/restore round-trip mid-vacancy. Ships as three independently live-verified commits (plus one test-flakiness fixup), same cadence as Phase 1.
**Resolved by:** `d5f5a6a` (day/schedule primitive + persistence, no behavior change), `0662670` (departure-intent extraction + proposal), `384d484` (fixed two build-time test flakiness issues surfaced by the Docker build's full-suite test gate — see below), `635fb4c` (scheduler execution + arrival seeding).
1. **`d5f5a6a`** — new `backend/app/engine/world_calendar.py` (`day_number()`, `PendingEvent`), `GameState.pending_events`, serialize/restore wiring (same pattern as beliefs/observation_log). `CastLifecycleState` gained a validated `replacement_timing` field (`immediate`|`next_day`, default `immediate`) and `departure_policy` validation (previously accepted any string silently). Pure additive/no-op — nothing yet reads `pending_events` or acts on `replacement_timing`. **Live result:** deploy healthy, gameplay unchanged.
2. **`0662670`** — `TurnExtraction` gained `departure_signal` (categorical `NONE|WISH|DECISION`, not a numeric confidence — "is this a joke or a decision" is a discrete judgment call). New `CastLifecycleState.propose_departure()` records intent to history without changing status (the "decision to leave next week is not immediate removal" state). New apply-pipeline block in `prompt_engine.py`: the hard guard against "a player cannot evict somebody merely by asserting they left" is code-level, not just a prompt rule — only `DECISION` signals naming a character currently in `lifecycle.active_ids()` are ever acted on. On a valid decision: proposes the departure, then schedules a `PendingEvent` for tomorrow (nothing executes yet — no scheduler exists in this commit). **Live result:** an in-character question that surfaced a WISH-shaped response ("everyone has, at some point... still figuring out") correctly produced no state change and the character remained present/engaged next turn; zero errors in deploy logs.
3. **`384d484`** (test-only fixup, no shipped-code change) — the Railway build runs the full pytest suite as a build gate, and two Phase 1 tests (`test_extraction_outbox_row_marked_done_after_successful_extraction`, `test_fact_extraction_recovery_sweep_reprocesses_pending_row`) failed there despite passing reliably and repeatedly locally. Root causes: a too-tight 1-second poll budget for a background task under build-environment CPU contention, and an assertion that implicitly assumed no other test's leftover rows existed in the shared ambient SQLite file that `_fact_extraction_recovery_sweep()` (by design) scans in full. Fixed both — widened the poll budget and filtered the assertion to the test's own row. **Lesson:** a build environment that runs the test suite as a gate can have different timing/scheduling characteristics than a local run; test assertions that assume exclusive ownership of shared ambient state (a real file-backed DB, not an isolated fixture) are a latent flakiness risk regardless of how many times they pass locally.
4. **`635fb4c`** — new `process_pending_events()` scheduler in `gameplay.py` (dependency-injects `apply_cast_replacement` to avoid a circular import), wired into `prompt_engine.py` right after `advance_time()`. On a fired arrival: seeds relationships via the *existing* `CharacterGraph.process_first_meetings()` and adds a one-turn arrival-intro transient marker that `prompt_builder.py` surfaces into the scene brief. **Bug found and fixed in the same commit:** `CharacterGraph.add_character()` — needed for `process_first_meetings()` to create a brand-new edge for an unauthored pair — was never called anywhere in the real codebase, so `graph.characters` was always empty and that whole code path was silently dead for every story, not just this one. An upcoming character like Arman has no authored edge to the player (they haven't arrived yet), so this was a real blocker, not a hypothetical one; fixed by registering the arrival room's participants from `state.characters` (already populated at newgame) right before the call. **Live result:** deploy healthy after a full-suite build gate pass; live-verified the Phase 1 regressions (solitude, future-resident unfamiliarity) still hold on the new build with zero errors in deploy logs. Full day-boundary/scheduler/introduction behavior against the real six_strangers config is verified by the local integration test suite (`test_pending_replacement_executes_on_scheduled_day_not_before`, `test_pending_replacement_survives_restart_mid_vacancy`, `test_newly_arrived_character_gets_introduction_and_seeded_relationships`, `test_empty_queue_departure_leaves_valid_vacancy_no_arrival`) rather than a live 24-hour-real-time or debug-tooling-gated check — a stronger and more repeatable guarantee than what's practically achievable live for a day-granularity mechanism.
- Tests added: `tests/backend/app/engine/test_world_calendar.py` (new file); extended `tests/backend/app/engine/test_cast_lifecycle.py` (departure_policy/replacement_timing validation, propose_departure); `tests/backend/app/engine/extractors/test_turn_extractor.py` (new file, departure_signal parsing); extended `tests/backend/app/engine/test_gameplay.py` (process_pending_events); extended `tests/backend/app/api/test_prompt_engine.py` (pending_events round-trip, wish-vs-decision, day-boundary execution, restart/retry mid-vacancy, arrival introduction + relationship seeding, empty-queue vacancy).
- Full suite at final commit: 572 passed, 1 xfailed, 0 failed.
**Deliberately not done:** Phase 3 (routines/commitments/goals) and Phase 4 (journal/recap/time-skip UI) from the audit's own phasing remain future milestones, now with `world_calendar.py`/`pending_events` available as the foundation Phase 3 was designed to reuse.

### Six Strangers audit Phase 1 — truthful scenes and durable state — FULLY COMPLETE (2026-09-19, source: [audit and proposal](SIX_STRANGERS_AUDIT_PROPOSAL_2026_09_19.md))
**Status:** all six items the audit's own "Phase 1" section calls for are done and live-verified: `SceneContext` context-selection policy, versioned dedup/idempotent turn commits (BL-02), and durable outbox + crash recovery for background extraction (BL-01b), on top of the original two live-bug fixes (future-resident leakage, solitude/focal-NPC override) and the atomic turn-commit ordering (BL-01's other half). Only BL-01c (mid-uptime recovery for a hung, non-crashed extraction task) remains open, and that's a deliberately narrower follow-on scoped after BL-01b shipped, not a gap in Phase 1's own acceptance criteria.
**Resolved by:** six commits on `beta`, in order: `6c90667` (initial Phase 1 fixes), `308f4bb` (roster closure), `0265297` (prompt-contract strengthening), `224856e` (actual root cause), `8e7991a` (SceneContext consolidation), `be199c5` (BL-02 turn-retry idempotency), `9c2878a` (BL-01b durable fact-extraction outbox). The first four were all required to close the two original live bugs — a worked example of why live verification (not just local tests) is part of done: the first three of those four each passed the full local suite and *looked* complete, but each still failed live re-verification on the deployed beta site, because the underlying bug had more than one contributing cause.
**What was found:** live beta contradicted the roster and world state in two ways: asking about an upcoming resident (Arman) confirmed his residency and exposed his authored private concern; an explicit solitary-scene request ("I go to the rooftop alone") still had the focal NPC (Mizuki) narrated as joining.
**What was done, round by round (each verified live against `https://beta-api.storieschat.ai`, `six_strangers`, guest sessions, before moving to the next round):**
1. **`6c90667`** — filtered lifecycle-ineligible characters out of the turn extractor's candidate catalog (`character_key_to_name` in `prompt_engine.py`) and out of canonical-fact rendering (`_fact_owner_only_upcoming()` in `prompt_builder.py`); gated `_character_identity_section`/`_storyteller_scene_section`'s main-character framing on scene presence via new `_main_character_scene_eligible()`; fixed the BL-01 save-ordering gap (`over`/`last_turn_*` now set before the session save) and added `beliefs`/`observation_log` to `_serialize_state`/restore; added `backend/app/config/build_info.py` for real commit/environment identity on `/api/version`, `/version.json`, `/beta/version.json`, `/api/health`. **Live result:** solitude check passed; Arman check still failed 3/3 — the model reliably said "he's one of the housemates... moving in soon" even with his private fact removed, because nothing told the narrator that an unrecognized name should read as genuinely unfamiliar rather than as a known-but-absent housemate.
2. **`308f4bb`** — added an explicit roster-closure instruction to the scene brief: lists the complete active cast by name and instructs that any name not on it is unfamiliar to every character. **Live result:** Arman check now passed 3/3 with correct genuine-unfamiliarity responses; but a *re-check* of solitude (done as part of the same verification pass) found Mizuki now walking onto the rooftop and speaking in 3/4 trials — the scene-brief-only gating from round 1 was insufficient once verified at higher sample size.
3. **`0265297`** — traced the solitude regression to `system_prompt()`'s always-injected STORYTELLER CONTRACT ("Keep {char_name} as the focal character...") and FOCAL STATE ("The focal character is {char_name}") sections, which unconditionally asserted main-character focus regardless of scene presence, directly contradicting the scene brief. Made both conditional on `_main_character_scene_eligible()`. **Live result:** Mizuki still appeared in 5/5 solitude trials — the instruction was now correctly present in the assembled prompt (confirmed locally) but a deeper data bug meant it wasn't even being triggered on the turn that mattered.
4. **`224856e`** — found the actual root cause: `_main_character_scene_eligible()`'s "no presence signal → assume main present" fallback couldn't distinguish a genuinely empty room (computed by the real turn pipeline right after travel resolves, `people_present == []`) from "presence has never been computed yet" (the true opening-turn case) — both produced an empty set from `_get_people_present_keys()`. Added `_scene_presence_has_been_computed()` to make that distinction explicit; the fallback now only fires when presence truly has no data yet. Also corrected a pre-existing test (`test_movement_preserves_combined_dialogue_and_destination_cast`) that had locked in the bug as expected behavior (asserted Mizuki's identity block MUST render in an empty room). **Live result:** solitude 8/8 clean (zero NPC mentions of any kind), Arman 4/4 correct — both fixes now hold at final verification.
5. **`8e7991a`** (2026-09-19, continuation) — consolidated the seven scattered scene-membership/knowledge-visibility functions (`_cast_scene_eligible`, `_get_active_character_keys`, `_get_people_present_keys`, `_get_scene_speaker_keys`, `_fact_owner_only_upcoming`, `_main_character_scene_eligible`, `_scene_presence_has_been_computed`) into a single `SceneContext` class (`backend/app/engine/scene_context.py`), per the audit's explicit recommendation for "one reusable context-selection policy" instead of point-patches. Behavior-preserving refactor: each free function now delegates to a `SceneContext` method (accepting an optional pre-built instance), with parity tests asserting byte-identical output against the prior implementation across lifecycle/non-lifecycle stories and both edge branches (presence-never-computed vs. presence-computed-empty-room) that the solitude fix depends on. **Live result:** re-ran both live repro checks against the redeployed beta site — solitude clean (no NPC joined an explicit alone-request), Arman genuinely unfamiliar (no residency confirmation) — confirming the consolidation introduced no regression.
6. **`be199c5`** (2026-09-19, BL-02 turn-retry idempotency) — added `game_sessions.last_request_id`/`last_reply_json` columns (via `_ensure_column()` in `database.py`, an additive `PRAGMA table_info` + `ALTER TABLE` guard since SQLite has no `ADD COLUMN IF NOT EXISTS`); `_chat_handler_impl` now checks an optional client-supplied `request_id` before any state mutation and replays the stored reply verbatim on a match instead of reprocessing; frontend `sendMessage()` sends a per-attempt `request_id` via the existing `genUUID()` helper. **Live result — first attempt looked broken:** duplicate-`request_id` requests were being fully reprocessed on beta (different replies, different token counts each time). Root cause was a test-harness mistake, not a code defect: the guest ID used in the live check (`bl02-verify-guest-<timestamp>`) didn't match `_extract_guest_id()`'s UUID-format validation (`backend/app/auth/dependencies.py`), so it silently fell back to `user_id="anon"` — and anon sessions are deliberately excluded from dedup (they never persist to SQLite at all). Re-ran with a real UUID guest ID and all three cases confirmed correct: guest duplicate `request_id` → byte-identical replayed reply; guest distinct `request_id` → processed as a new turn; anon duplicate `request_id` → reprocessed (matches the by-design anon exclusion). **Lesson:** a live-verification "failure" can itself be a test-setup bug — before concluding the deployed fix is broken, check whether the request actually exercised the code path under test (here: whether the guest header was even valid enough to produce a non-anon `user_id`).
7. **`9c2878a`** (2026-09-19, BL-01b durable fact-extraction outbox) — added a `fact_extraction_outbox` table (new `CREATE TABLE IF NOT EXISTS`, no migration needed); `FactExtractionOutboxRepo` (enqueue/mark_done/mark_failed/fetch_pending/prune_done_older_than); for non-anon sessions, `_extract_and_store` now durably enqueues before firing the in-process background extraction task and marks the row done/failed after; `backend/app/main.py`'s `lifespan` runs a one-shot startup recovery sweep that reprocesses any row left `pending` from a prior crash and prunes `done` rows older than 7 days. Anon sessions keep the legacy best-effort fire-and-forget path (no durable identity to key a row on). **Live result:** deploy succeeded (proving the new startup DB query + recovery sweep don't crash-loop the app on the real Railway persistent volume), `/api/health` clean, no exceptions in deploy logs; sent a real guest turn on beta and confirmed no enqueue/persist failures logged and gameplay (including the solitude/unfamiliarity fixes from earlier rounds) unaffected. True crash-mid-extraction recovery fidelity (row genuinely left `pending` by a real process crash, not a test-inserted row) was not forced on the live deploy — that's the audit's own noted limit ("server-backed restart fidelity remains to be proved") and is intentionally not required to call this piece done, since local tests already exercise that exact path end-to-end (`test_fact_extraction_recovery_sweep_reprocesses_pending_row` and siblings in `tests/backend/app/test_main.py`).
- Tests added across the four commits: `tests/backend/app/api/test_prompt_engine.py` (`test_upcoming_character_private_facts_excluded_from_canonical_stack`, `test_turn_extractor_catalog_excludes_upcoming_and_departed_characters`, `test_main_character_identity_not_injected_when_absent_from_scene`, `test_main_character_identity_unaffected_for_non_lifecycle_story`, `test_turn_commit_persists_over_and_last_turn_fields_from_same_turn`, `test_beliefs_and_observation_log_round_trip_through_restore`, `test_scene_brief_closes_roster_against_upcoming_characters`, `test_system_prompt_forbids_main_character_entering_solitary_scene`); `tests/backend/app/test_main.py` (`test_version_endpoint`/`test_health_endpoint` extended, `test_version_json_endpoints_expose_build_identity` added). Commit 5 added `test_scene_context_parity_with_free_functions_lifecycle_story`, `test_scene_context_parity_solitary_and_never_computed_states`, `test_scene_context_parity_non_lifecycle_story`, `test_turn_extractor_catalog_still_uses_scene_context_eligibility`. Commit 6 added `test_init_db_adds_dedup_columns_to_existing_database`, `test_get_last_request_returns_none_for_new_session`, `test_update_and_get_last_request_round_trips`, `test_create_or_update_session_without_request_id_preserves_prior_dedup_token` (`tests/backend/app/db/test_repos.py`), `test_duplicate_request_id_replays_prior_reply_without_reprocessing`, `test_different_request_id_still_processes_normally`, `test_anon_session_skips_dedup_check` (`tests/backend/app/api/test_prompt_engine.py`). Commit 7 added `test_enqueue_creates_pending_row`, `test_mark_done_removes_row_from_pending`, `test_mark_failed_increments_attempts_and_stores_error`, `test_prune_done_older_than_removes_only_old_done_rows` (`tests/backend/app/db/test_repos.py`), `test_extraction_outbox_row_enqueued_for_guest_session`, `test_extraction_outbox_row_marked_done_after_successful_extraction`, `test_anon_session_skips_outbox_enqueue` (`tests/backend/app/api/test_prompt_engine.py`), `test_fact_extraction_recovery_sweep_reprocesses_pending_row`, `test_fact_extraction_recovery_sweep_marks_row_failed_on_error`, `test_fact_extraction_recovery_sweep_prunes_old_done_rows` (`tests/backend/app/test_main.py`).
- Full suite at final commit: 532 passed, 1 xfailed, 0 failed.
**Lesson:** a fix that passes local unit tests and looks complete by code inspection can still have live-only failure modes with more than one contributing cause layered on top of each other (prompt-content bug, then a competing-instruction bug, then a data/fallback-logic bug) — each surfaced only by actually re-exercising the exact live repro steps after each fix, not by re-reading the code. This is the `/ship-and-verify` skill's Phase 3 working as intended.
**Deliberately not done (see BL-01c):** mid-uptime recovery for a hung-but-not-crashed extraction task; Phase 2 (cast-cycling-as-playable-feature: departure proposals in the turn extractor, scheduler, arrival relationship seeding), Phase 3 (routines/commitments/goals), and Phase 4 (journal/recap/time-skip UI) from the audit's own phasing are explicitly out of scope — these are later phases the audit itself sequences after Phase 1, not gaps in Phase 1.

### BL-09 — `/api/chat` leaked the full system prompt to every player (found + fixed 2026-09-17, source: live playtest verification of The Common Room, /ship-and-verify skill Phase 3)
**Resolved by:** `446477b` (`fix(security): stop /api/chat leaking the full system prompt to players`), pushed to `beta`.
**What was found:** while manually verifying The Common Room actually worked on the live deployed beta site (curling `https://beta-api.storieschat.ai/api/chat` directly, not just running the local test suite), the response included an unconditional `prompt_debug.system_prompt_preview` field containing the FULL assembled system prompt for that turn — every `canonical_facts` entry (a mystery's actual solution), every character's private secrets, `belief_seeds`, and retrieval chunk text — for any caller, no auth needed, on every single turn. This wasn't caught by the earlier A03 fix (session this same day) because A03 gated separate debug/authoring *routes*; it never touched this field on the always-public `/api/chat` route. The player-facing `[D]` bracket command (an unauthenticated toggle anyone can send) gates a much less sensitive `debug_box`, and is NOT an auth boundary — conflating the two would not have fixed this.
**What was done:** added `is_operator_request()` (non-raising sibling to `require_operator`) in `backend/app/auth/dependencies.py`; `/api/chat`'s response now only includes `prompt_debug` when the caller presents a valid operator token, same trust boundary as the A03-gated routes. `backend/app/api/debug_engine.py`'s own internal `/api/chat` calls (which legitimately need `prompt_debug` for grading/playback) now forward the operator token they already hold from `require_operator_ws`. Also fixed a related previously-undetected regression the A03 pass introduced: the grader's story-context fetch was calling the now-operator-gated `/api/stories/{id}/context` with no token at all, silently 401ing on every graded run since A03 shipped. Two new tests added (`test_prompt_debug_not_leaked_to_ordinary_players`, `test_prompt_debug_present_for_authenticated_operator`) in `tests/backend/app/api/test_prompt_engine.py`. Full suite: 473 passed, 1 xpassed, 0 failed at the time of this fix (479 after Six Strangers landed alongside it).
**Lesson:** this is exactly the class of bug `.claude/skills/ship-and-verify/SKILL.md` (added same session) exists to catch — it was invisible to the full local pytest suite and only surfaced by actually curling the live deployed endpoint as a real caller would.

### BL-07 — `character_self_knowledge` is architecturally single-character (source: social_sim mode + Common Room work, 2026-09-17)
**Resolved by:** `e3a38d8` (`fix(engine): resolve BL-07 - character_self_knowledge is now per-character`), pushed to `beta`.
**What was done:** `Character.from_dict()`/`to_dict()` in `backend/app/engine/state.py` now parse/serialize a per-character `self_knowledge` field (previously silently dropped into `meta` and never read). Both character-roster-construction sites in `backend/app/api/prompt_engine.py` (new-game path, restore path) now prefer a character's own `self_knowledge` when present, falling back to the legacy top-level `character_self_knowledge` array only for the `is_main` character. `_character_identity_section()` in `backend/app/engine/prompt_builder.py` now injects one identity block per qualifying character: the main character's block stays unconditional (unchanged legacy behavior), and every other present character (via `_get_people_present_keys`) with their own `self_knowledge` also gets a block. `backend/app/stories/6_common_room/common_room_story.json` was updated so all three housemates (Mina, Dae-ho, Priya) now author their own `self_knowledge`, demonstrating genuinely ensemble character identity. Backward compatibility with all 5 pre-existing mystery stories is regression-tested (byte-identical single-block output for `iu_murder_mystery`). Full suite: 469 passed, 1 xpassed, 0 failed, 0 skipped.

### BL-03 — Beta/prod isolation via `/beta/` path (source: audit A15, P1 investigation) — CONFIRMED then fixed
**Resolved by:** live-verified via curl (`https://storieschat.ai/api/stories` lacked `common_room`; `https://beta-api.storieschat.ai/api/stories` had it, confirming the beta page was actually reaching prod), then fixed in `frontend/index.html`.
**What was done:** this was upgraded from "unverified" to a confirmed live bug: `frontend/index.html` computed `API_BASE = window.location.origin + "/api"`, so a page loaded from `https://storieschat.ai/beta/` called `https://storieschat.ai/api/...`, which Cloudflare routes to the **prod** Railway origin, not beta — meaning the hosted beta UI's chat feature was silently talking to prod. `frontend/debug.html` already had the correct fix for this exact problem (`_isBeta` detection → `beta-api.storieschat.ai`); `index.html` did not, despite already having a partial version of it for OAuth only (`LOGIN_AUTH_BASE`). Fix: introduced `BACKEND_ORIGIN`, computed the same way `LOGIN_AUTH_BASE` already was, and derived `API_BASE`/`AUTH_BASE` from it instead of a bare `window.location.origin`; also fixed `getStoriesEndpointCandidates()`'s fallback, which previously hardcoded the prod URL unconditionally (would have silently re-introduced the bug on any primary-fetch failure from a beta page). Added `tests/frontend/test_beta_api_base.py` (no JS test harness exists in this repo, so this is a structural/string-level regression guard on the shipped file, not a real browser test). Full suite: 471 passed, 1 xpassed, 0 failed, 0 skipped.
